Priority,Task,Status,Notes
1,Fix Grok API 400 Bad Request errors,In Progress,Current main blocker
2,Fix Data Retainer 403 error (data collection),Blocked,Needs working /api/collect-data
3,Make Sovereign & Lords return reliable JSON,Pending,Depends on #1
4,Pass real market data from Data Retainer → Sovereign,Pending,Depends on #2
5,"Improve frontend log display (colors, actors, scrolling)",Pending,Nice-to-have
6,"Clean up GitHub (remove .DS_Store, old files, etc.)",Pending,Do after system works
7,Add proper error handling & retry logic,Pending,Future improvement

Error,What Caused It,Lesson Learned
"ModuleNotFoundError (drone_wars.config, models.llm_client, grok, etc.)",Bad imports + missing __init__.py + wrong Python path,Always set sys.path correctly and use proper package structure
NameError: name 'app' is not defined,Decorators before app = Flask(...),Define Flask app before any routes
Grok API 400 Bad Request,Payload formatting + response_format + weak prompts,xAI is stricter than OpenAI. Use very explicit instructions and avoid forcing response_format unless prompt is perfect
403 on data collection,Endpoint didn't exist or was broken,Always create working endpoints before calling them from agents
Environment variables not loaded,.env not loaded at runtime,Load .env very early with load_dotenv(override=True)
Port 5000 in use,macOS AirPlay Receiver,Use port 5001 for dev
IndentationError,Mixed tabs/spaces + bad copy-paste,Use consistent 4-space indentation

Key Principles We Should Follow Going Forward:

Test in isolation — Test API keys, endpoints, and LLM calls separately before integrating.
One source of truth — No duplicate orchestrators (JS vs Python).
Fail fast & log loudly — Every major step should log clearly.
Strict JSON discipline — When asking for JSON, be extremely explicit and use fallback parsing.
Use real pipeline — Don't reinvent data collection. Use whinfell_pipeline/agent_wrapper.py.
Virtual environment always — Never install globally for this project.