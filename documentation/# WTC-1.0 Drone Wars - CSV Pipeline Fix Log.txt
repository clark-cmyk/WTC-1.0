# WTC-1.0 Drone Wars - CSV Pipeline Fix Log

**Date**: July 13, 2026
**Branch**: fix-real-data-pipeline
**Goal**: Get `run_csv_download.py` running and feeding real data into DroneWars core.

## Summary of Journey

### Key Errors Encountered
- `ModuleNotFoundError: No module named 'china_policy_track'`
- `ModuleNotFoundError: No module named 'china_policy_track.china_ladder'`
- `ModuleNotFoundError: No module named 'china_policy_track.models'`
- `ModuleNotFoundError: No module named 'china_policy_track.sq3'`
- `TypeError: cmd_daily() missing required keyword arguments`
- `TypeError: unsupported operand type(s) for /: 'str' and 'str'`
- `ImportError: cannot import name 'ChinaPolicyObservation' from 'china_policy_track.models'`
- `AttributeError: 'bool' object has no attribute 'parent'`
- Missing `yaml` package

### Failed Fixes
- Multiple full `cat > core.py` and `run_csv_download.py` overwrites
- PYTHONPATH hacks in subprocess
- `pip install -e whinfell_pipeline` (no setup.py)
- Simple `__init__.py` shims

### Successful Fixes
- Copied `china_ladder.py` from archive
- Copied `models.py` + other files from `global_track` into `china_policy_track`
- Added proper proxy `__init__.py` in `china_policy_track`
- Added `sys.path` fix in `run_csv_download.py`
- Used `Path` objects for arguments to `cmd_daily`
- Installed `pyyaml`

### Current Status
- `run_csv_download.py daily` now runs without import errors
- Pipeline executes but finds no local data files (file-only mode)
- LLM calls are working with `grok-4.3`

### TODO
- Make `_collect_data()` in `core.py` read the generated CSV/manifest and pass real data to Sovereign/panels
- Wire UI Refresh button to trigger full cycle
- Handle "no local CSV supplement" errors (add initial data fetch or fallback)
- Clean up duplicate/shim files in `china_policy_track` / `global_track`
- Commit and push to GitHub

**Next Action**: Update `core.py` to consume real CSV output.


####

# WTC 1.0 - How to Create Run Environment

**Last Updated**: July 13, 2026

## 1. Setup Venv
```bash
cd ~/Desktop/wtc_1.0
python3 -m venv venv
source venv/bin/activate

pip install flask httpx python-dotenv pyyaml pandas

GROK_API_KEY=xai-...

4. Path Fixes

Ensure whinfell_pipeline and china_policy_track are in PYTHONPATH
Run PYTHONPATH=. for tests

5. Run Commands

CSV only: PYTHONPATH=. python run_csv_download.py daily
Full cycle: PYTHONPATH=. python -m drone_wars.core

Common Issues

Missing yaml → pip install pyyaml
Key not loaded → load_dotenv()
Import errors → Check sys.path and archive files

TODO: Add Barchart/Koyfin API keys when ready.

Open it:

```bash
open WTC_1.0_Run_Environment_Setup.md


###


cd ~/Desktop/wtc_1.0
grep -n "refresh\|panel\|drone\|json" wtc_1.0.html | head -10


List by code line 



