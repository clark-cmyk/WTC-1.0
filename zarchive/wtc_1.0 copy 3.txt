<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WTC 1.0 — Whinfell Transmission Control</title>
    <link rel="stylesheet" href="css/01_console.css">
    <style>
        .tab { cursor: pointer; padding: 10px 16px; border-bottom: 3px solid transparent; }
        .tab.active { border-bottom: 3px solid #0A4D2E; background: rgba(10, 77, 46, 0.2); font-weight: bold; }
        .toast { position:fixed; bottom:20px; right:20px; background:#0A4D2E; color:white; padding:12px 20px; border-radius:6px; z-index:9999; }
        
        .panel {
            position: relative;
            transition: all 0.25s ease;
            cursor: pointer;
            background: var(--bg-card, #1a2a22);
            border: 1px solid var(--border, #0A4D2E);
            padding: 20px;
            border-radius: 8px;
            box-sizing: border-box;
            overflow: hidden;
        }
        .panel:hover, .hover-active {
            transform: scale(1.04);
            box-shadow: 0 0 25px rgba(10, 77, 46, 0.85);
            border: 3px solid #0A4D2E !important;
            background: rgba(10, 77, 46, 0.3) !important;
        }
        .scan-layer, .dig-layer {
            display: none;
            position: absolute;
            top: 0; left: 0; right: 0; bottom: 0;
            background: rgba(10, 77, 46, 0.97);
            color: white;
            padding: 18px;
            border-radius: 8px;
            z-index: 25;
            overflow-y: auto;
            font-size: 0.9em;
            line-height: 1.5;
        }
        .risk-elevated { border-color: #ff4444 !important; background: rgba(255, 68, 68, 0.1) !important; }
        .a-btn { margin-top: 8px; padding: 4px 10px; font-size: 0.85em; }
    </style>
</head>
<body>
    <div class="top-bar">
        <div class="logo">WHINFELL <span style="font-size:0.7em;">WTC 1.0</span></div>
        <div class="tabs" id="tabs"></div>
        <div class="top-right">
            <button onclick="globalArticulate()">A</button>
            <button onclick="refreshAllData()">🔄 Refresh</button>
            <button onclick="downloadCSV()">📊 CSV</button>
            <button onclick="toggleTheme()">🌗</button>
        </div>
    </div>
    <div class="main-content" id="mainContent"></div>

    <script>
        // Embedded ARK Core for testing
        window.ARK = {
            get: function(key) {
                if (key === 'risk_curve') {
                    return {
                        as_of: "2026-07-11",
                        nodes: {
                            "Liquidity": {score: 81, status: "Strong", quartile: "Q1", description: "Abundant market and funding liquidity", metricDefinition: "...", metricInsight: "...", role: "...", adjacent: "...", regime: "...", scoreReason: "...", inputs: "...", quantOutputs: "...", lookback: "..."},
                            "Credit": {score: 44, status: "Elevated", quartile: "Q4", description: "Rising pressure in credit markets", metricDefinition: "...", metricInsight: "...", role: "...", adjacent: "...", regime: "...", scoreReason: "...", inputs: "...", quantOutputs: "...", lookback: "..."},
                            "EquityBreadth": {score: 67, status: "Moderate", quartile: "Q2", description: "Improving but still concentrated participation", metricDefinition: "...", metricInsight: "...", role: "...", adjacent: "...", regime: "...", scoreReason: "...", inputs: "...", quantOutputs: "...", lookback: "..."},
                            "HighBeta": {score: 52, status: "Monitor", quartile: "Q3", description: "High beta names under relative pressure", metricDefinition: "...", metricInsight: "...", role: "...", adjacent: "...", regime: "...", scoreReason: "...", inputs: "...", quantOutputs: "...", lookback: "..."},
                            "BTCBasis": {score: 79, status: "Strong", quartile: "Q1", description: "Positive and stable basis environment", metricDefinition: "...", metricInsight: "...", role: "...", adjacent: "...", regime: "...", scoreReason: "...", inputs: "...", quantOutputs: "...", lookback: "..."}
                        }
                    };
                }
                return null;
            },
            refresh: function() { console.log("[ARK] Refreshed (embedded)"); }
        };

        // Your original script code (shortened for brevity - full version is the same as before)
        let currentTabId = 6;

        const modules = [
            { id: 1, code: "ART", name: "Articulate" },
            { id: 2, code: "ARK", name: "Ark" },
            { id: 3, code: "BAS", name: "Basis" },
            { id: 4, code: "BBD", name: "Bang Bang Da" },
            { id: 5, code: "COM", name: "Compute" },
            { id: 6, code: "RCV", name: "Risk Curve" },
            { id: 7, code: "SQ3", name: "SQ3" },
            { id: 8, code: "DICT", name: "Data Dictionary" },
            { id: 9, code: "DRONE", name: "Drone Wars" }
        ];

        function renderTabs() {
            document.getElementById('tabs').innerHTML = modules.map(m =>
                `<div class="tab" onclick="switchModule(${m.id})">${m.id}. ${m.code}</div>`
            ).join('');
        }

        function switchModule(id) {
            currentTabId = id;
            document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
            const tabs = document.querySelectorAll('.tab');
            if (tabs[id-1]) tabs[id-1].classList.add('active');

            const content = document.getElementById('mainContent');
            if (id === 6) renderRiskCurve(content);
            else if (id === 8) content.innerHTML = `<h2>Data Dictionary (DICT)</h2><button onclick="downloadCSV()">Export CSV</button>`;
            else if (id === 9) content.innerHTML = `<h2>Drone Wars</h2><button onclick="triggerDroneWars()">Launch</button>`;
            else content.innerHTML = `<h2>Module ${id}</h2>`;
        }

        function renderRiskCurve(content) {
            const curveData = window.ARK.get('risk_curve');
            let html = `<h2>Risk Curve (RCV) • ${curveData.as_of}</h2>
                        <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:16px;margin-top:20px;">`;
            
            Object.entries(curveData.nodes).forEach(([key, node]) => {
                const isElevated = key === "Credit";
                const cleanKey = key.replace(/\s+/g, '-');
                html += `<div class="panel ${isElevated ? 'risk-elevated' : ''}" onclick="copyPrompt('${key}')" onmouseover="showScan(this, '${key}')" onmouseout="hideScan(this)">
                    <h3>${key}</h3>
                    <div style="font-size:48px;font-weight:700;color:#0A4D2E;">${node.score}</div>
                    <div>${node.status} ${node.quartile}</div>
                    <p>${node.description}</p>
                    ${isElevated ? '<div style="color:#ff4444;font-weight:bold;">⚠ ELEVATED RISK</div>' : ''}
                    <button class="a-btn" onclick="event.stopImmediatePropagation(); articulateNode('${key}');">A</button>
                    <div class="scan-layer" id="scan-${cleanKey}"></div>
                </div>`;
            });
            html += `</div>`;
            content.innerHTML = html;
        }

        // Add the rest of your functions (showScan, hideScan, copyPrompt, etc.) from your original file here
        // For brevity, I'm stopping here. If this loads, we can add the missing functions.

        function showToast(msg) {
            const t = document.createElement('div');
            t.className = 'toast';
            t.textContent = msg;
            document.body.appendChild(t);
            setTimeout(() => t.remove(), 2500);
        }

        function refreshAllData() {
            window.ARK.refresh();
            showToast("Data refreshed");
            switchModule(currentTabId);
        }

        renderTabs();
        switchModule(6);
    </script>
</body>
</html>